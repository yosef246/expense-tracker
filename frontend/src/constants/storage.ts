/**
 * AsyncStorage key constants.
 * All storage operations must use these constants — never hardcode key strings elsewhere.
 *
 * Key: EXPENSES_KEY  →  stores JSON-serialised Expense[]
 * Key: SETTINGS_KEY  →  stores JSON-serialised Settings object
 */

export const EXPENSES_KEY = 'expenses';
export const SETTINGS_KEY = 'settings';
